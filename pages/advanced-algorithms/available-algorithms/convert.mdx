---
title: convert
description: Learn how to convert data types using procedures from the MAGE library.  
---

# convert

import { Card, Cards } from 'nextra/components'
import GitHub from '/components/icons/GitHub'

Module used for conversion of one data type to another.

<Cards>
  <Card
    icon={<GitHub />}
    title="Source code"
    href="https://github.com/memgraph/mage/blob/main/python/convert.py"
  />
</Cards>

| Trait               | Value      |
| ------------------- | ---------- |
| **Module type**     | util       |
| **Implementation**  | Python     |
| **Parallelism**     | sequential |

## Function

### `str2object()`

Inline function that converts the input string to an object the string
represents, using Python's `json.dumps` function.

{<h4> Input: </h4>}

- `string: string` ➡ The input string that will be converted.

{<h4> Output: </h4>}

- `mgp.Any` ➡ Since the function is inline, the output is not named, and can be of various data types, depending of the content of the string.

{<h4> Usage: </h4>}

In this case, the string will be converted to a list:

```cypher
RETURN convert.str2object("[1,2,3]") AS result;
```

```plaintext
+----------------------------+
| result                     |
+----------------------------+
| [1, 2, 3]                  |
+----------------------------+
```

In this use case, the string will be converted into a map, containing a list:

```cypher
RETURN convert.str2object('{"key": [1,2,3]}') AS result;
```

```plaintext
+----------------------------+
| result                     |
+----------------------------+
| {"key": [1,2,3]}           |
+----------------------------+
```
